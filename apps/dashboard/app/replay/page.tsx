import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function ReplayPage() {
    return (
        <PlaceholderPage
            title="Replay"
            description="Manual replay controls and replay history will live here."
            phase="Phase 5"
        />
    );
}